# 1011

## Python

```python
T = int(input())

for _ in range(T):
    x, y = map(int, input().split())
    d = y - x
    cnt = 0
    move = 1
    while 0 < d:
        if cnt % 2 == 1:  # 같은 숫자 한 번 더가기
            move -= 1
        d -= move
        move += 1
        cnt += 1
    print(cnt)
```
